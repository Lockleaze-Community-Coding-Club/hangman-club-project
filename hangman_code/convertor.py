 # This recieves HTTP/HTTPS messages from the user and converts them into a format that can be used by main
 # It also converts output from main into HTTP/HTTPS messages and sends then to the user

def recieve_request ():
     #Recieves requests from user and sends to main by converting HTTP/HTTPS into python
        return None


def send_request ():
     #Recieves requests from main, converts from python to HTTP/HTTPS and sends back the user
        return None